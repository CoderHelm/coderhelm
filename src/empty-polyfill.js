// Intentionally empty — modern browsers don't need these polyfills.
// Our browserslist targets last 2 versions of Chrome/Firefox/Safari/Edge,
// all of which natively support: Array.prototype.at, flat, flatMap,
// Object.fromEntries, Object.hasOwn, String.prototype.trimStart/trimEnd.
